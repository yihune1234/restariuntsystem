# Dependencies
node_modules/

# Environment — NEVER commit real secrets.
# Copy `.env.example` to `.env` and fill in real values locally.
.env
.env.*
!.env.example

# Logs
logs/
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*

# Test / build output
coverage/
dist/
build/

# Runtime uploads (user-uploaded food images, etc.) — do not commit
/uploads/

# OS / editor
.DS_Store

# Temp
.tmp/
*.tmp
